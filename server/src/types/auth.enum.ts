export enum ETokenType {
  ACCESS = "access",
  REFRESH = "refresh",
  EXAM = "exam",
}
export enum EUserRole {
  ADMIN = "admin",
  USER = "user",
  STUDENT = "student",
}
//["student", "teacher", "admin"]
